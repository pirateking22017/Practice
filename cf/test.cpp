#include<iostream>
#include<algorithm>
#include<vector>

using namespace std;

void splitting_items(){
    int size_of_array = 2;
    cin >> size_of_array;
    vector<int> array_a(size_of_array);
    for (int i = 0; i < size_of_array; i++)
    {
        cin>>array_a[i];
    }
    int cnt_queries =1 ; cin >> cnt_queries;
    
    
    
}
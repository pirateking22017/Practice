#include <bits/stdc++.h>
using namespace std;
/*
void solve(){
    int tot_pop;
    cin >> tot_pop;
    vector <double> each_wealth;
    for(int i=0; i<tot_pop; i++){
        cin >> each_wealth[i];
    }
    double sum = 0;
    for (double value:each_wealth){
        sum+=value;
    }
    double avg_wealth = sum/each_wealth.size();
    int count_of_unhappy = 0;
    for(int i=0; i<tot_pop; i++){
        if(each_wealth[i]<avg_wealth) {count_of_unhappy++;}  
    }
    count_of_unhappy>tot_pop/2 ? c
}
*/

void solve(){
    int n;
    cin >> n;
    vector <long long> v(n);
    
}

int main(){
    int tc;
    cin >> tc;
    while(tc--){solve();}
}